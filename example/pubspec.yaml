# Copyright (c) 2026 Piergiorgio Vagnozzi.
# Licensed under the MIT License.
name: platform_serial_example
description: Professional serial terminal example application for platform_serial.
publish_to: none

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  platform_serial:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
